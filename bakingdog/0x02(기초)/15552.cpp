#include <bits/stdc++.h>
using namespace std;

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);
	int f, s, n;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> f >> s;
		cout << f + s << "\n";
	}
}